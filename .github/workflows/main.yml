name: env var

on: 
    deployment:
        branches: main

jobs:
    my-job:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v3
        env:
            GITHUB_TOKEN: ${{ secrets.API_NINJAS_TOKEN }}
